DATAS SEGMENT
    DATA DB 76,69,84,90,73,88,99,63,100,80
    LEN EQU $-DATA
    S6 DB 00H
    S7 DB 00H
    S8 DB 00H
    S9 DB 00H
    S10 DB 00H 
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    
    MOV DL,10
    MOV BX,0
AGAIN:
    CMP BX,LEN
    JA ENDD
    MOV AL,BYTE PTR DATA[BX]
    INC BX
    MOV AH,0
    CMP AL,100
    JE ADD10
    DIV DL
    CMP AL,6
    JE ADD6
    CMP AL,7
    JE ADD7
    CMP AL,8
    JE ADD8
    CMP AL,9
    JE ADD9
    JMP AGAIN
    
ADD6:
    MOV AL,BYTE PTR S6
    INC AL
    MOV BYTE PTR S6,AL
    JMP AGAIN

ADD7:
    MOV AL,BYTE PTR S7
    INC AL
    MOV BYTE PTR S7,AL
    JMP AGAIN    

ADD8:
    MOV AL,BYTE PTR S8
    INC AL
    MOV BYTE PTR S8,AL
    JMP AGAIN
    
ADD9:
    MOV AL,BYTE PTR S9
    INC AL
    MOV BYTE PTR S9,AL
    JMP AGAIN
    
ADD10:
    MOV AL,BYTE PTR S10
    INC AL
    MOV BYTE PTR S10,AL
    JMP AGAIN        
    
ENDD:
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START