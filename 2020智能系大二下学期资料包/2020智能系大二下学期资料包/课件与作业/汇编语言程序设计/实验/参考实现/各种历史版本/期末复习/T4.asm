DATAS SEGMENT
    DATA1 DB 30H
    DDD EQU 8
    DATA2 DB 38H
    
    BVAR DB 10H,20H,30H,40H
DDVAR:
    WVAR DD 12345678H
    STRING DB '1234$'
    STR EQU $-STRING-2*2  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    
    MOV CX,3
    MOV AX,0
    LAB:
    INC AX
    LOOP LAB
    
    MOV AL,(7-0)*2
    
    MOV AX,STR
    
    MOV AX,DDVAR
    MOV BX,DDVAR+4
    MOV AL,BVAR
    MOV AX,WORD PTR WVAR+2
    MOV DX,WORD PTR STRING+2
    
    
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START