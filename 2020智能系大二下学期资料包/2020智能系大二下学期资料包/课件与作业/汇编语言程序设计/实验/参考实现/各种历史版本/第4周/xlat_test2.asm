DATAS SEGMENT
    TAB DB '0123456789ABCDEF'  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    
    MOV BX,OFFSET TAB
    MOV AL,4
    XLAT
    
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
