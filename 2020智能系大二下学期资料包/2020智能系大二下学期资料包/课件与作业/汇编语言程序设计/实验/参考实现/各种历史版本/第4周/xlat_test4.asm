DATAS SEGMENT
    TAB DB '11111111111111111111111111',24H
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

SSEG    SEGMENT PARA STACK 
        DW  256 DUP(?)
SSEG    ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    
    MOV BX,OFFSET TAB
    MOV AL,1
    
    INT 21H
    XLAT
    MOV AL,4CH
    INT 21H
    
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
