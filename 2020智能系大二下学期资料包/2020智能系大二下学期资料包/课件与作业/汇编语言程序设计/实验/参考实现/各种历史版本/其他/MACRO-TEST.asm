DATAS SEGMENT
    DATA DB 'HERE',0DH,0AH,'$'  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
PRINT MACRO A
    LEA DX,A
    MOV AH,9
    INT 21H
    ENDM
    MOV AX,DATAS
    MOV DS,AX
    
    PRINT [1]
    
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START